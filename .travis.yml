language: node_js

sudo: required

before_install:
  - nvm install 9.10.1
  - npm i -g npm@5.6.0

notifications:
  email:
    recipients:
      - gil.maricruz@gmail.com
    on_success: always # default: change
on_failure: always # default: always
